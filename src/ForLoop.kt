class ForLoop {
    static void main (String[] args)

    String [] catBoard = new String[] {"karmi", "Bubu", "Simba", "Tuflik", "Parker"};
    for (int i = 0; i < catBoard.lenght; i++) {
        System.out.printIn(catBoard[i]);
    }
    for (string cat: catBoard) {
        System.out.printIn(cat);
    }
